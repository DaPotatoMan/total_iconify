name: example
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  total_iconify:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# https://dart.dev/tools/pub/pubspec
flutter:
  uses-material-design: true

  # assets:
  #   - packages/total_iconify/assets/
